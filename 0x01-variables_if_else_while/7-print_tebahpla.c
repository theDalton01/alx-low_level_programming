#include <stdio.h>
/**
 * main - Entry point
 * To print the alphabets in reverse
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char letter;

	if (letter = 'z'; letter >= 'a';)
		putchar(letter);

	puchar('\n');
	return (0);
}

